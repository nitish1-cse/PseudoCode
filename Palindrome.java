public class Palindrome {
/*
DECLARE NUM := INTEGER
DECLARE TEMP := INTEGER
DECLARE REM := INTEGER
DECLARE REV := INTEGER
READ NUM
SET TEMP = NUM
SET REV = 0
WHILE NUM > 0
    REM = NUM % 10
    REV = REV * 10 + REM
    NUM = NUM / 10
END WHILE

IF TEMP = REV THEN
    PRINT "PALINDROME"
ELSE
    PRINT "NOT PALINDROME"
END IF
*/
    public static void main(String[] args) {
        
    }
}
